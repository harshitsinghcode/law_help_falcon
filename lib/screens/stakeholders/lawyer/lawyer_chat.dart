import 'package:flutter/material.dart';

class LawyerChat extends StatelessWidget {
  const LawyerChat({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Title'),
      ),
      body: Container(),
    );
  }
}
